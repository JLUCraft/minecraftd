# Rust Project Coding Standard

## Philosophy

* 一切类型必须显式。
* 一切状态必须显式。
* 一切错误必须显式。
* 优先编译期约束，避免运行期约束。
* 优先简单设计，避免过度设计。
* 优先组合，避免继承式抽象。
* 模块必须单一职责。
* 不允许为未来假设需求设计系统。
* 所有修改必须保持项目风格一致。
* 提交代码必须达到零警告、零错误。

## Compiler

* rustfmt 必须通过。

* cargo check 必须零警告。

* cargo clippy 必须零警告。

* cargo test --all-targets 必须通过。

* 编译警告视为错误。

* CI 失败禁止合并。

* 允许使用宏提升抽象层次。

* 禁止使用宏隐藏业务逻辑。

## Type System

* 函数参数和返回类型必须显式声明。
* 业务状态必须使用 enum。
* 错误码必须使用 enum。
* 领域对象必须使用 NewType。
* 禁止使用 String 表示业务状态。
* 禁止使用裸整数表示业务状态。
* 禁止 Magic Number。
* 禁止 Magic String。
* 避免 impl Trait，除非 Trait 边界。
* 禁止 dyn Trait，除非接口边界必须。
* 优先泛型静态分发。
* 优先编译期保证而非运行期检查。

## Error Handling

* 禁止 unwrap()。
* 禁止 expect()。
* 禁止 panic!() 处理业务逻辑。
* 禁止吞异常。
* 禁止忽略 Result。
* 所有错误必须进入统一错误体系。
* 使用`Result<T, E>` 或 `Option<T>`。
* 错误类型统一使用 thiserror。
* 应用层统一使用 anyhow 聚合上下文。
* 错误必须保留完整上下文链路。
* 客户端不得暴露内部异常信息。

## State Management

* 禁止 static mut。
* 优先不可变变量。
* 状态变化必须可追踪。
* 多线程共享状态必须使用 Mutex、RwLock 或消息传递。
* 优先消息传递而非共享内存。
* 禁止隐藏状态修改。
* 状态转换必须显式定义。

## State Machine

* 业务状态必须使用 Enum。
* 状态机必须集中定义。
* 状态流转必须显式声明。
* 禁止任意状态跳转。
* 禁止直接修改状态字段。
* 状态转换必须经过状态机接口。
* 状态机变更必须评审。
* 状态转换必须具备测试。

## Module

* 每个模块职责单一。
* 每个 crate 职责单一。
* 仅导出必要公共 API。
* 优先 private。
* 禁止循环依赖。
* 禁止工具模块无限膨胀。
* 文件命名使用单个完整单词。
* 模块边界必须稳定。

## Naming

* Struct、Enum、Trait 使用 PascalCase。
* 函数、变量、方法使用 snake_case。
* 常量使用 UPPER_CASE。
* 同一业务概念全项目统一命名。
* API 字段命名统一。
* 数据库字段命名统一。
* Redis Key 命名统一。
* 禁止同义词混用。

## Configuration

* 禁止硬编码配置。
* 配置统一来源。
* 使用 TOML 管理配置。
* 敏感信息必须来自环境变量。
* 常量使用 const。
* 全局共享配置使用 once_cell。
* 配置变更必须可审计。

## Database

* 所有查询必须分页。
* 禁止无限制列表查询。
* 禁止 SELECT *。
* 所有 SQL 必须参数化。
* 禁止字符串拼接 SQL。
* 禁止 N+1 查询。
* 所有慢查询必须记录。
* 查询必须评估索引命中情况。
* JsonB 仅允许扩展字段。
* 核心业务字段禁止存入 JsonB。
* 主键策略必须统一。
* 事务模式必须统一。
* 数据删除策略必须统一。
* 所有数据库操作必须设置超时。
* 软删除策略必须统一定义。
* 禁止同一项目同时存在多种分页模型。

## API

* API 响应结构统一。
* API 错误结构统一。
* API 分页结构统一。
* 输入参数命名统一。
* 输出字段命名统一。
* 接口必须版本化。
* 禁止重复功能接口。
* 禁止为假设场景增加接口。
* JSON 层级结构保持稳定。
* 同一资源仅允许一种表达方式。

## Security

* 默认拒绝访问。
* 所有接口必须权限校验。
* 所有资源必须所有权校验。
* 所有输入默认不可信。
* 验证码接口必须限流。
* 登录接口必须防暴力破解。
* Token 必须使用安全随机源。
* 禁止可预测随机数。
* 支付金额必须服务端计算。
* 严禁硬编码后门。
* 严禁测试后门进入生产环境。
* 敏感操作必须记录审计日志。

## Concurrency

* 所有共享资源必须考虑竞态条件。
* 幂等接口必须保证幂等性。
* 分布式锁必须设置超时。
* 重试必须设置退避策略。
* 禁止无限重试。
* 并发修改必须保证一致性。
* 关键业务必须评估并发风险。

## Logging & Observability

* 全项目只允许一种日志框架。
* 使用 tracing。
* 日志必须结构化。
* 日志必须包含 TraceId。
* 日志字段统一。
* 禁止 println! 进入生产代码。
* 敏感信息禁止记录日志。
* 核心链路必须可追踪。
* 日志采集规范必须统一。
* 慢请求必须可观测。

## Dependency

* 禁止引入废弃依赖。
* 禁止引入无人维护依赖。
* 禁止同功能多套依赖。
* 所有依赖必须有明确用途。
* 未使用依赖必须删除。
* 定期执行 cargo audit。
* 保持最小依赖原则。

## Testing

* 单元测试覆盖边界情况。
* 状态机必须测试。
* 错误路径必须测试。
* 测试不得依赖执行顺序。
* 测试数据必须隔离。
* 失效测试必须删除。
* 重构后必须通过全量测试。
* cargo test --all-targets 必须通过。
* 测试代码必须与生产代码同步维护。

## Unsafe Policy

* 默认禁止 unsafe。
* 每个 unsafe 必须附带安全性证明。
* unsafe 必须经过独立 Review。
* unsafe 数量必须持续监控。
* 能不用 unsafe 就不用 unsafe。

## AI Coding Rules

* AI 不得扩大需求范围。
* AI 不得增加未要求 API。
* AI 不得新增命名体系。
* AI 不得新增状态体系。
* AI 不得新增错误体系。
* AI 不得绕过项目架构。
* AI 修改后必须通过编译。
* AI 修改后必须通过测试。
* AI 不得提交不可运行代码。
* AI 不得谎报完成状态。
* AI 重构必须保持系统可运行。
* AI 批量修改必须具备回滚方案。
* AI 完成任务前必须验证结果。

## Absolute Prohibitions

* 禁止 unsafe（经审批例外）。
* 禁止 unwrap()。
* 禁止 expect()。
* 禁止 panic!() 处理业务逻辑。
* 禁止 static mut。
* 禁止 Magic Number。
* 禁止 Magic String。
* 禁止 N+1 查询。
* 禁止权限绕过。
* 禁止硬编码后门。
* 禁止吞异常。
* 禁止死代码。
* 禁止未分页查询。
* 禁止不可回滚变更。
* 禁止未验证交付。
